#ifndef BOIDMANAGER_H
#define BOIDMANAGER_H

class BoidManager {
public:
    BoidManager();
    ~BoidManager();

    void update();
    void render();

private:
    // Add private members here
};

#endif // BOIDMANAGER_H